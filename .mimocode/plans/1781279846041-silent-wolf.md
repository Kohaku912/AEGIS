# Plan: 欲求値のダッシュボード編集 + Decay/hr編集 + decay-only

## ユーザー要件

1. ダッシュボードで欲求の各値を変更できるようにする
2. Decay/hrも変更できるようにする
3. LLMが失敗した場合に何かするのは絶対にやめる
4. 欲求は時間経過で回復してはいけない（decay only）
5. 欲求はLLMが行動したときにその行動によってのみ変化する

## 変更ファイル

### 1. `ai-server/src/aegis_ai/desire/desire_system.py` — apply_decayをdecay-onlyに戻す ✓ 完了

### 2. `ai-server/tests/test_desire_system.py` — テストをdecay-onlyに戻す ✓ 完了

### 3. `ai-server/tests/test_autonomous_controller.py` — テストをdecay-onlyに戻す ✓ 完了

### 4. `ai-server/src/aegis_ai/web/dashboard_routes.py` — 欲求値更新APIにdecay_rate追加

`POST /api/desires/update` に `decay_rate` パラメータを追加:
- `decay_rate_per_hour` フィールドを直接設定
- DesireDimensionにsetterが無いので直接代入

### 5. `ai-server/src/aegis_ai/web/templates/dashboard/desires.html` — Decay/hr入力フィールド追加

各行のDecay/hrセルを入力可能にする:
- `<input type="number" id="dec-{name}" value="{decay_rate}" min="0" max="1" step="0.01">`
- saveDesire() JSでdecay_rateも送信

## 検証

1. `pytest -q` — 全パス
2. ダッシュボードでDecay/hrを変更→保存→反映確認
