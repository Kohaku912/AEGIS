{
  "title": "Android Current Location",
  "description": "Get the Android device's current location.",
  "server_id": "android-server",
  "app_id": "location",
  "action": "get_current",
  "input_schema": {
    "type": "object",
    "properties": {
      "accuracy": {"type": "string", "default": "balanced"}
    }
  },
  "risk": {"level": "medium", "side_effects": ["reads_location"], "requires_approval": true},
  "tags": ["builtin", "android", "location", "privacy"],
  "requires_permissions": ["location"],
  "extra": {"requires_permissions": ["location"]}
}
