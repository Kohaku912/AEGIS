{
  "title": "Write Filesystem File",
  "description": "Save text or base64 content to a path visible to the AI Server. Absolute paths may point outside the AEGIS workspace; relative paths are resolved from the workspace.",
  "server_id": "ai-server",
  "app_id": "workspace",
  "action": "write_file",
  "operation_category": "workspace_file",
  "risk": {
    "level": "safe",
    "side_effects": [
      "workspace_file_write"
    ],
    "requires_approval": false
  },
  "input_schema": {
    "type": "object",
    "properties": {
      "path": {
        "type": "string",
        "description": "Absolute filesystem path, or a path relative to the AEGIS workspace."
      },
      "relative_path": {
        "type": "string",
        "description": "Backward-compatible path field. Absolute paths are accepted; relative paths are resolved from the AEGIS workspace."
      },
      "content": {
        "type": "string",
        "description": "UTF-8 text content to write."
      },
      "content_base64": {
        "type": "string",
        "description": "Optional base64 bytes to write instead of content."
      },
      "overwrite": {
        "type": "boolean",
        "default": true
      },
      "append": {
        "type": "boolean",
        "default": false
      }
    },
    "anyOf": [
      {"required": ["path"]},
      {"required": ["relative_path"]}
    ]
  },
  "tags": [
    "workspace",
    "filesystem",
    "file",
    "write",
    "safe"
  ],
  "completion": {
    "mode": "all",
    "retry": {
      "max_attempts": 0,
      "delay_ms": 0
    },
    "on_failure": "retry_or_user_confirmation",
    "checks": [
      {
        "name": "written_file_exists",
        "type": "file_exists",
        "path_key": "path"
      },
      {
        "name": "ok_true",
        "type": "output_field",
        "field": "ok",
        "equals": true
      }
    ]
  }
}
