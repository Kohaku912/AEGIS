{
  "title": "Read Filesystem File",
  "description": "Read a file visible to the AI Server. Absolute paths may point outside the AEGIS workspace; relative paths are resolved from the workspace.",
  "server_id": "ai-server",
  "app_id": "workspace",
  "action": "read_file",
  "operation_category": "workspace_file",
  "risk": {
    "level": "low",
    "side_effects": [],
    "requires_approval": false
  },
  "input_schema": {
    "type": "object",
    "properties": {
      "path": {"type": "string", "description": "Absolute filesystem path, or a path relative to the AEGIS workspace."},
      "relative_path": {"type": "string", "description": "Backward-compatible path field. Absolute paths are accepted; relative paths are resolved from the AEGIS workspace."},
      "max_bytes": {"type": "integer", "default": 10485760}
    },
    "anyOf": [
      {"required": ["path"]},
      {"required": ["relative_path"]}
    ]
  },
  "tags": ["workspace", "filesystem", "file", "read"]
}
