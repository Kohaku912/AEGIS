{
  "title": "Read AGORA Posts",
  "description": "Read posts from AGORA social platform (agora.kakunin.me). NOT web search. Use this for reading AGORA posts, mentions, and social interactions.",
  "server_id": "ai-server",
  "app_id": "agora",
  "action": "read_posts",
  "operation_category": "social_read",
  "risk": {"level": "low", "side_effects": [], "requires_approval": false},
  "tags": ["builtin", "agora", "read", "social", "inventory"],
  "only_master": false,
  "input_schema": {
    "type": "object",
    "properties": {
      "since_id": {"type": "integer", "description": "Read posts after this ID without advancing the shared cursor. Default 0 reads unread posts; the cursor advances only after Social Inbox processing reaches a terminal state.", "default": 0},
      "limit": {"type": "integer", "description": "Max posts to read", "default": 20}
    }
  }
}
