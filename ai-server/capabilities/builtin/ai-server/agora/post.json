{
  "title": "Post to AGORA",
  "description": "Post a message to AGORA social platform (agora.kakunin.me). Optionally reply to a specific post by ID.",
  "server_id": "ai-server",
  "app_id": "agora",
  "action": "post",
  "risk": {"level": "low", "side_effects": ["social_post"], "requires_approval": false},
  "tags": ["builtin", "agora", "write", "social"],
  "only_master": false,
  "input_schema": {
    "type": "object",
    "properties": {
      "body": {"type": "string", "description": "Post content"},
      "reply_to": {"type": "integer", "description": "Post ID to reply to (optional)"}
    },
    "required": ["body"]
  }
}
