{
  "title": "Browse with AI",
  "description": "Legacy unspecialized browser automation. Prefer a split browser-server capability. Because intent is ambiguous, this compatibility capability executes with audit.",
  "server_id": "browser-server",
  "app_id": "page",
  "action": "browse",
  "operation_category": "external_send",
  "risk": {
    "level": "audited_action",
    "side_effects": [
      "sends HTTP request",
      "executes page JavaScript"
    ],
    "requires_approval": false
  },
  "tags": [
    "browser",
    "web",
    "ai",
    "automation"
  ],
  "examples": [
    {
      "user": "ブラウザでexample.comを開いて",
      "arguments": {
        "task": "Open https://example.com and report the page title"
      }
    },
    {
      "user": "サイトを見て内容を確認して",
      "arguments": {
        "task": "Open the requested site and summarize the visible content"
      }
    }
  ],
  "only_master": false,
  "input_schema": {
    "type": "object",
    "properties": {
      "task": {
        "type": "string",
        "description": "Detailed task description including URL and what to do. For complex tasks like account creation, include all required details (name, username, password, etc.). Example: 'Go to https://accounts.google.com/signup and create account with first name Taro, last name Yamada, username taro.test.2026, password Str0ngP@ss!'"
      },
      "max_steps": {
        "type": "integer",
        "description": "Maximum number of steps the agent can take (default: 50). Increase for complex multi-page flows.",
        "default": 50
      }
    },
    "required": [
      "task"
    ]
  },
  "completion": {
    "mode": "all",
    "retry": {
      "max_attempts": 0,
      "delay_ms": 0
    },
    "on_failure": "retry_or_user_confirmation",
    "checks": [
      {
        "name": "browser_completed",
        "type": "output_field",
        "field": "status",
        "equals": "COMPLETED"
      }
    ]
  }
}
