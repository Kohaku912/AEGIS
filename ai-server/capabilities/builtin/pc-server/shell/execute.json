{
  "id": "pc-server.shell.execute",
  "title": "Execute Shell Command",
  "description": "Execute a shell command (executes with audit)",
  "server_id": "pc-server",
  "app_id": "shell",
  "action": "execute",
  "operation_category": "general",
  "risk": {
    "level": "high",
    "requires_approval": false
  },
  "input": {
    "type": "object",
    "properties": {
      "command": {
        "type": "string",
        "description": "Command to execute"
      },
      "working_dir": {
        "type": "string",
        "description": "Working directory"
      }
    },
    "required": [
      "command"
    ]
  },
  "tags": [
    "shell",
    "execute",
    "audited_action"
  ],
  "tcp_command": "execute_shell {command}|{working_dir}"
}
