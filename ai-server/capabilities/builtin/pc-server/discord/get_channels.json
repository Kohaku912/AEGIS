{
  "title": "Discord Channels",
  "description": "List Discord channels visible in one server/guild through Discord RPC.",
  "server_id": "pc-server",
  "app_id": "discord",
  "action": "get_channels",
  "operation_category": "social_read",
  "input_schema": {
    "type": "object",
    "properties": {
      "guild_id": {
        "type": "string",
        "description": "Discord guild/server id"
      }
    },
    "required": ["guild_id"]
  },
  "risk": {
    "level": "low",
    "requires_approval": false
  },
  "tags": ["discord", "guild", "channel", "read_only"],
  "tcp_command_json": "discord_get_channels"
}
