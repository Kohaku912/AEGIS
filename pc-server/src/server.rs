//! gRPC server (stub — not yet integrated with proto stubs).
//! Will be generated when proto codegen is set up for Rust.

pub mod grpc_stub {
    // Placeholder for generated gRPC code.
    // Run: `cargo build` after adding tonic-build to build.rs
}
